const religion = "islam"
var hairstyle= "wolfcut"
let move = "lahore"
car = "civic"// var can also
//  be declared like this but we will not use this method tis not professional
let year;

console.table([religion,hairstyle,move,car,year])

/*
var will be used in the old js because ehen we change one name in int code the ithers will be changes automatically there so we use let instead of var now


*/